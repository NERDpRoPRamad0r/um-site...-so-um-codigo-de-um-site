from flask import Flask, render_template
import random

app = Flask(__name__)

lista = ["Voce sabia que o besouro tigre, é tao rapido mas tao rapido que fica cego quando corre?"
        "Voce sabia que a terra nao é completamente redonda? Sim, ela possui alguns cantos achatados"
        "Voce sabia que existem bolhas espacias? sim, inclusive agente ta dentro de uma delas!"
        "Voce sabia que a maioria das pessoas que sofre de dependência tecnológica sente um forte estresse quando fica fora da área de cobertura de rede ou não pode usar seus dispositivos"
        "Voce sabia que Elon Musk afirma que as redes sociais são projetadas para nos manter dentro da plataforma, fazendo com que passemos o máximo de tempo possível consumindo conteúdo"
]

@app.route("/")
def hello_world():
    return '<h1>Hello, World!</h1>'

@app.route("/pietro")
def pietro():
    return '<h2>pietro italiano</h2>'

@app.route("/fatos")
def fatos():
    global lista
    return f'<p>{random.choice(lista)}</p>'

@app.route('/kodland')
def kodland():
    '<h1>recomendo a kodland, foi dali que aprendi python! é tao legal que vou elogiar a kodland.</h1>'
    '<h2>kodland é util</h2>'
    '<h3>kodland é dahora</h3>'
    '<h4>kodland é legal</h4>'

app.run(debug=True)
    '<h5>kodland é curso do sucesso</h5>'
    '<h5>kodland muda vidas</h5>'
    '<h6>kodland nao é só uma escola com varios cursos, kodland É UMA ESCOLA COM VARIOS CURSOS</h6>'
